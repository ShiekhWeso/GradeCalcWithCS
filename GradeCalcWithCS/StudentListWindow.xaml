<Window x:Class="GradeCalcWithCS.StudentListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GradeCalcWithCS"
        mc:Ignorable="d"
        Title="StudentListWindow" Height="450" Width="800">
    <Grid Margin="10">
        <StackPanel>

            <DockPanel Margin="0,0,0,10">
                <TextBlock Text="All Students"
                           FontSize="20"
                           FontWeight="Bold"
                           VerticalAlignment="Center"
                           DockPanel.Dock="Left"/>
                <Button Content="Refresh"
                        Click="Refresh_Click"
                        Width="80"
                        Height="28"
                        Background="#FF2196F3"
                        Foreground="White"
                        FontWeight="SemiBold"
                        HorizontalAlignment="Right"
                        Margin="10,0,0,0"
                        DockPanel.Dock="Right"/>
            </DockPanel>

            <ComboBox x:Name="SortOption"
                      Margin="0,10,0,10"
                      SelectionChanged="SortOption_Changed"
                      Width="200">
                <ComboBoxItem Content="Sort by GPA (High to Low)" IsSelected="True"/>
                <ComboBoxItem Content="Sort by Name (A–Z)"/>
            </ComboBox>

            <ListView x:Name="StudentListView" Margin="0,5,0,0">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Name" Width="200" DisplayMemberBinding="{Binding Name}"/>
                        <GridViewColumn Header="GPA" Width="80" DisplayMemberBinding="{Binding GPA}"/>
                        <GridViewColumn Header="Percentage" Width="100" DisplayMemberBinding="{Binding Percentage}"/>
                    </GridView>
                </ListView.View>
            </ListView>

        </StackPanel>
    </Grid>
</Window>