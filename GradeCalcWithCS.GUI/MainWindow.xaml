<Window x:Class="GradeCalcWithCS.GUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GradeCalcWithCS.GUI"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Student Name:" Grid.Row="0" FontWeight="Bold"/>
        <TextBox Name="StudentNameBox" Grid.Row="1" Margin="0,5,0,10"/>

        <TextBlock Text="Grade:" Grid.Row="2" FontWeight="Bold"/>
        <TextBox Name="GradeBox" Grid.Row="3" Margin="0,5,0,10"/>

        <StackPanel Grid.Row="4" Orientation="Vertical">
            <Button Content="Calculate GPA" Click="CalculateGPA_Click" Margin="0,10,0,10"/>
            <TextBlock Name="ResultText" FontSize="14" FontWeight="Bold" Foreground="DarkGreen"/>
        </StackPanel>
    </Grid>

</Window>
